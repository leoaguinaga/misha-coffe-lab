---
import "../styles/global.css";
import FooterSection from "../components/sections/FooterSection.astro";
import Header from "../components/sections/Header";
import Analytics from "@vercel/analytics/astro";

interface NavItem {
  label: string;
  href: string;
}

interface Props {
  title?: string;
  description?: string;
  navItems?: readonly NavItem[];
  ctaLabel?: string;
  ctaHref?: string;
  showFooter?: boolean;
}

const {
  title = "MISHA | Café de Especialidad",
  description = "Café de especialidad de origen, trazable y preparado por expertos en Chiclayo.",
  showFooter = true,
} = Astro.props;
---

<!doctype html>
<html lang="es" class="scroll-smooth">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
    <Analytics />
  </head>
  <body
    class="bg-background-light font-sans text-secondary antialiased relative"
  >
    <Header />
    <main>
      <slot />
    </main>
    {showFooter && <FooterSection />}
  </body>
</html>
